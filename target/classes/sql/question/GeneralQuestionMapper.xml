<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="generalQuestionMapper">

	<insert id="write" parameterType="generalQuestion">
		<selectKey keyProperty="questionNo" resultType="int" order="BEFORE">
			SELECT
			wl_general_question_SEQ.nextval FROM dual
		</selectKey>
		INSERT INTO wl_general_question (question_no, question, answer)
		VALUES(#{questionNo},#{question},#{answer})
	</insert>
	
	<delete id="delete" parameterType="string">
 		delete from wl_general_question where no=#{value}
 	</delete>
	
	<select id="getGeneralQuestionList" resultType="generalQuestion" parameterType="string">
 		<!-- select question_no, question, language, cnt_submit, cnt_right, page from
		(select question_no, question, language, cnt_submit, cnt_right, ceil(rownum/5) page from 
		(select question_no, question, language, cnt_submit, cnt_right
		from wl_algorithm_question
		
		order by question_no))
		where page = #{value}  -->
		select question_no, question, cnt_submit, cnt_right
		from wl_general_question
	</select>
		
	
	<select id="showContent" resultType="generalQuestion" parameterType="string">
 		select question_no, question, answer, cnt_submit, cnt_right
 		from wl_general_question
 		where question_no=#{value}
 	</select>

	<update id="updateSubmit" parameterType="string">
		update wl_general_question set cnt_submit=cnt_submit+1 where
		question_no=#{value}
	</update>
	
	<update id="updateRight" parameterType="string">
		update wl_general_question set cnt_right=cnt_right+1
		where question_no=#{value}
	</update>

</mapper>


















