<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="algoQuestionMapper">

	<insert id="write" parameterType="algorithmQuestion, member">
		<selectKey keyProperty="questionNo" resultType="int" order="BEFORE">
			SELECT
			wl_algorithm_question_SEQ.nextval FROM dual
		</selectKey>
		INSERT INTO wl_algorithm_question (question_no, question, input, output, language, answer, user_id)
		VALUES(#{questionNo},#{question},#{input},#{output},#{language},#{answer}, user_id)
	</insert>
	
	<delete id="delete" parameterType="string">
 		delete from wl_algorithm_question where no=#{value}
 	</delete>
	
	<select id="getAlgorithmQuestionList" resultType="algorithmQuestion" parameterType="string">
 		<!-- select question_no, question, language, cnt_submit, cnt_right, page from
		(select question_no, question, language, cnt_submit, cnt_right, ceil(rownum/5) page from 
		(select question_no, question, language, cnt_submit, cnt_right
		from wl_algorithm_question
		
		order by question_no))
		where page = #{value}  -->
		select question_no, question, language, cnt_submit, cnt_right
		from wl_algorithm_question
	</select>
		
	
	<select id="showContent" resultType="algorithmQuestion" parameterType="string">
 		select question_no, question, input, output, language, answer, cnt_submit, cnt_right
 		from wl_algorithm_question
 		where question_no=#{value}
 	</select>

	<update id="updateSubmit" parameterType="string">
		update wl_algorithm_question set cnt_submit=cnt_submit+1 where
		question_no=#{value}
	</update>
	
	<update id="updateRight" parameterType="string">
		update wl_algorithm_question set cnt_right=cnt_right+1
		where question_no=#{value}
	</update>
	
	<select id="getAnswer" resultType="string" parameterType="string">
		select answer
		from wl_algorithm_question
		where question_no=#{value}
	</select>

</mapper>


















